projection;
strict (2);
use draft;

define behavior for ZCIT_VICKY_C_HDR alias SalesOrderHdr
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;

  use action uploadExcelData;
  use action DownloadExcel;

  use association _salesitem { create; with draft; }
}

define behavior for ZCIT_VICKY_C_ITM alias SalesOrderItm
{
  use update;
  use delete;

  use association _salesHeader { with draft; }
}